# Scribe v2.0 Requirements
# CPU-optimized Glass Box AI Agent for TheCommons

# Core dependencies (required)
requests>=2.31.0
psutil>=5.9.0
llama-cpp-python>=0.2.0

# Optional: GPU acceleration (uncomment based on your hardware)
# torch>=2.0.0  # For NVIDIA CUDA
# mlx>=0.13.0  # For Apple Metal (macOS only)

# Optional: API fallback
# groq>=0.4.0  # Groq API fallback (requires GROQ_API_KEY)

# Optional: Model management
# huggingface-hub>=0.20.0  # For automatic model downloads

# Development
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0
