# Requirements for TheCommons v2.0 - Scribe & Scripts

# Core dependencies (CPU-only, lightweight)
requests>=2.31.0
psutil>=5.9.0

# Scribe v2.0 - AI Brain & Stability Auditor
# CPU-native inference with llama-cpp-python
llama-cpp-python>=0.2.0

# Optional: GPU acceleration
# Uncomment based on your hardware:
# torch>=2.0.0; python_version >= "3.8"  # For CUDA/NVIDIA GPU
# torch-metal>=2.0.0; platform_system == "Darwin"  # For Apple Metal

# Optional: Groq API fallback (requires GROQ_API_KEY env variable)
# groq>=0.4.0

# Optional: Hugging Face model downloader
# huggingface-hub>=0.20.0

# Development dependencies
pytest>=7.0.0
